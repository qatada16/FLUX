<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- READ_SMS only: balances are recovered by scanning the system inbox on
       demand, so we never need RECEIVE_SMS (a live broadcast) at all. -->
  <uses-permission android:name="android.permission.READ_SMS" />
</manifest>
